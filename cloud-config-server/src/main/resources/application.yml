#server:
#  port: 1111
spring:
  application:
    name : config-server​
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xjt520/spring-cloud-app
          search-paths: cloud-config-repo
server:
  port: 7001

